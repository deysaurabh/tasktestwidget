version: 1.0
runtime: python311
pre-run:
  - pip3 install pipenv
  - pipenv install
build:
  commands:
    build:
      - pip3 install -r requirements.txt
run:
  runtime-version: 3.11
  command: python3 app.py
  network:
    port: 5000
    env: PORT